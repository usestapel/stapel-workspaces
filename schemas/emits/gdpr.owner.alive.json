{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdpr.owner.alive",
  "description": "Answer to gdpr.owner.probe: this module's erasure subscriber is running and claims these subject types. Read by stapel-gdpr's DataOwnerHealth, GET /gdpr/api/v1/owners/health and the gdpr.W006 boot check.",
  "type": "object",
  "required": ["owner", "subject_types"],
  "properties": {
    "owner":          { "type": "string", "const": "workspaces" },
    "subject_types":  { "type": "array", "items": { "type": "string" } },
    "correlation_id": { "type": "string" }
  },
  "additionalProperties": false
}
