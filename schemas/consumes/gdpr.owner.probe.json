{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdpr.owner.probe",
  "description": "Liveness probe from stapel-gdpr's probe_data_owners beat. Answered with gdpr.owner.alive from the same subscriber that handles gdpr.erasure.requested, so an answer proves the erasure path is consumed rather than that a container is running.",
  "type": "object",
  "required": ["correlation_id"],
  "properties": {
    "correlation_id": { "type": "string", "format": "uuid" }
  },
  "additionalProperties": false
}
