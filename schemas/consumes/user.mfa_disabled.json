{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "user.mfa_disabled",
  "description": "The user lost their LAST strong second factor (account-level transition — mirror of user.mfa_enabled). Emitted from TOTP disable/force-disable (incl. the delayed-change execute task) and passkey deactivation.",
  "type": "object",
  "required": ["user_id", "factor"],
  "properties": {
    "user_id": { "type": "string", "format": "uuid" },
    "factor":  { "type": "string" }
  },
  "additionalProperties": false
}
