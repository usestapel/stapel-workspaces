{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdpr.erasure.requested",
  "description": "stapel-gdpr opened an erasure for one subject. This module claims subject_type account and workspace; anything else is another owner's and is ignored without a receipt. Handled together with gdpr.owner.probe in actions.py — the probe answer is only evidence because it comes from the same subscriber.",
  "type": "object",
  "required": ["correlation_id", "subject_type", "subject_key"],
  "properties": {
    "request_id":     { "type": "integer" },
    "correlation_id": { "type": "string", "format": "uuid" },
    "subject_type":   { "type": "string", "examples": ["account", "workspace"] },
    "subject_key":    { "type": "string" },
    "workspace_id":   { "type": "string" },
    "requested_by":   { "type": "string" },
    "origin":         { "type": "string" },
    "due_at":         { "type": "string", "format": "date-time" }
  },
  "additionalProperties": false
}
