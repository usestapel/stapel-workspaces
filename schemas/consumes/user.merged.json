{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "user.merged",
  "description": "Consumed from stapel-auth. An anonymous guest account was absorbed into an existing account; the guest row ceases to exist. Workspaces re-owns the guest's workspaces and memberships onto the surviving account, demoting the guest's personal workspace so the survivor keeps exactly one. Contract owned by the emitter.",
  "type": "object",
  "required": ["from_user_id", "into_user_id"],
  "properties": {
    "from_user_id": { "type": "string" },
    "into_user_id": { "type": "string" },
    "reason": { "type": "string" }
  },
  "additionalProperties": true
}
