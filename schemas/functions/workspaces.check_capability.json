{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "workspaces.check_capability",
  "description": "Does the user hold this capability in the workspace? Resolved against the effective role registry (builtin + STAPEL_WORKSPACES['ROLES'] overlay), wildcards included; only accepted memberships count. Returns {allowed: bool, role: str|null}.",
  "type": "object",
  "required": ["workspace_id", "user_id", "capability"],
  "properties": {
    "workspace_id": { "type": "string", "format": "uuid" },
    "user_id":      { "type": "string", "format": "uuid" },
    "capability":   { "type": "string", "minLength": 1 }
  },
  "additionalProperties": false
}
